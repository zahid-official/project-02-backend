// Enum for Role
enum UserRole {
    ADMIN
    DOCTOR
    PATIENT
}

// Enum for Status
enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
}

// Enum for Gender
enum Gender {
    MALE
    FEMALE
}

// Enum for Appointment Status
enum AppointmentStatus {
    SCHEDULED
    INPROGRESS
    COMPLETED
    CANCELED
}

// Enum for Payment Status
enum PaymentStatus {
    PAID
    UNPAID
}
